spring:
  cloud:
    discovery:
      enabled: false
    config:
      enabled: false
      discovery:
        enabled: false
  redis:
    host: localhost
    port: 6379
  application:
    name: authentication-service

jwt:
  secret: my-secret
