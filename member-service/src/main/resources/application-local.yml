spring:
  cloud:
    discovery:
      enabled: false

  jpa:
    show-sql: on
    hibernate:
      ddl-auto: create

  datasource:
    url: jdbc:h2:~/e-commerce-local

jwt:
  secret: my-secret
server:
  port: 8082