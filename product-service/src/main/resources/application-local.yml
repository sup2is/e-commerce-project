spring:
  cloud:
    discovery:
      enabled: false

  jpa:
    show-sql: on
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.H2Dialect

  datasource:
    url: jdbc:h2:~/e-commerce-local

jwt:
  secret: my-secret
server:
  port: 8083
